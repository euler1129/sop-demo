# 演示脚本 · SOP 流程资产线上化平台

> 适用对象：客户流程管理 / 信息化负责人、业务部门流程 owner
> 演示时长：**12 分钟讲 + 8 分钟问答**
> 开场一句话：**"你们现在散落在各个开发人员手里的流程图，可以不用再画了。"**

---

## 0. 开场（30 秒）

> "我们了解到，贵司现在大量的流程资产——Excel 的环节方案、Visio 的泳道图、Word/PDF 的作业指引，
> 实际是**散落在各个开发人员手里**的。谁画的谁才看得懂，改一次要找一次人，版本对不上是常态。
> 今天演示的这套平台，做的就一件事：**把这些存量文件一次性变成可管理、可复用、可发布的系统资产。**"

---

## 1. 第一步：把客户的真实文件导进来（2 分钟）

**操作**：顶部「导入」→ 选择 `A-集输港业务流程环节方案V3.1.xlsx`

**话术**：
- 这是贵司现有的集港流程方案表，26 行环节节点、合并单元格、单元格里还混着"情况1/情况2/异常/产生费用"——
  系统**不是简单读表格**，而是把缩进层级拆成了分支、异常、费用三类结构化字段。
- 导入耗时显示在底部状态栏（毫秒级）。

**操作**：再导入 `客户A海运流程-20260226.vsdx`

**话术**：
- Visio 是最难的一类：3 页泳道图、237 个形状、102 条连线。
- 系统读的不是图片，是 OPC 包里的 XML：靠**容器关系 + 坐标**还原泳道归属，靠 **Connect 的起止单元格**还原连线，
  靠 Visio 自带的 **Function 属性**还原每个节点属于哪个业务环节。
- 结果是：9 个环节、7 条角色泳道、47 个节点、51 条连线，一秒出图。

> **关键点**：这两个文件是**客户的真实文件**，不是我们造的。

---

## 2. 第二步：同一份资产，四种视图（2 分钟）

**操作**：依次点「泳道流程图 / BPMN 2.0 / 价值链图 / 资产表格」

| 视图 | 讲什么 |
| --- | --- |
| 泳道图 | 自动布局，不用手动对齐；节点按**承载系统**着色；异常节点红色描边+警示徽标，费用节点金色徽标 |
| BPMN 2.0 | **行业标准**。多情况自动转成并行网关，异常自动转成排他网关 + 异常分支任务 |
| 价值链图 | 给领导看的阶段级视图：一眼看清主链路和每个角色的参与密度 |
| 资产表格 | 给流程 owner 补全元数据的视图：缺什么字段标红，可直接在表里补 |

**话术**：
> "同一份数据，四种视角：执行层看泳道图，IT 看 BPMN，管理层看价值链，流程 owner 补数据在表格里。
> 以前这四张图要画四遍，现在**改一次、四处同步**。"

---

## 3. 第三步：导出标准 BPMN，接你们自己的引擎（1 分钟）

**操作**：BPMN 视图 →「导出 BPMN XML」

**话术**：
- 导出的是**标准 BPMN 2.0 XML**，带完整的 `BPMNDiagram` 图形信息，
  Camunda / Flowable / Activiti / 金蝶 / 用友等引擎可以直接打开。
- 反过来，外部系统的 `.bpmn` 文件也能拖进来被平台识别。
- 这一步对应甲方需求里写的"通过 BPMN2.0 标准快速生成流程引擎流程"——**不是导图片，是导可执行的流程定义**。

---

## 4. 第四步：一键生成 SOP 文档（2 分钟）

**操作**：顶部「一键生成 SOP 文档」→ 预览 →「导出 Word」

**话术**：
- 从流程图直接生成标准 SOP 文档：封面、版本记录、环节总览表、节点明细表、
  操作说明、异常与费用清单、涉及系统清单，共 7 段。
- 导出的是 `.docx`，贵司自己的模板可以再套；也可以直接打印成 PDF。
- 以前维护一份 SOP 文档要 2 天，现在**点一下**。

---

## 5. 第五步：AI 三件套（3 分钟）

### 5.1 自然语言建模
**操作**：「自然语言建模」→ 现场口述一段流程 → 生成
> "把脑子里/会议里的流程直接说出来，系统直接建出泳道图。这对**还没画过图的流程**特别有用。"

### 5.2 文档智能解析
**操作**：导入 Word / PDF 存量 SOP
> "Word 按标题与表格抽环节，PDF 抽文本后按规则切分。
> **PDF 如果是内嵌 CID 字体的中文（扫不出来），系统会明确告诉你解析不了，并引导改用 Word/Excel——我们不做假数据。**"

### 5.3 合规与优化诊断
**操作**：看右侧「AI 诊断」清单，点一条问题
**话术**：
- 11 条规则：缺失角色、缺失输入输出、缺失承载系统、异常分支无回退、
  数据契约断链、主状态断链、分支未闭环、孤立节点、环节缺节点、角色过载、跨系统未标注集成点。
- 点任意一条，**画布自动定位并脉冲高亮**对应节点。
- 这就是"导入之后马上知道还差什么"，把 60 分的流程资产快速补到 90 分。

**必须说清的一句话**（诚信，也是卖点）：
> "现在默认是**离线规则引擎**在跑，结果标注为『离线规则引擎』，我们不会把规则输出伪装成 AI 生成。
> 如果贵司有大模型，可以在设置里填 OpenAI 兼容接口，诊断会叠加一层大模型建议——**内网信创环境也能部署，这是我们相对 SaaS 竞品的优势**。"

---

## 6. 第六步：版本管理与差异对比（1 分钟）

**操作**：「版本对比」→ 存快照 → 改几个节点 → 再对比

**话术**：
- 流程是活的：AS-IS 现状版、TO-BE 优化版、系统集成版，都能留版本。
- 对比是**节点级**的：新增 / 删除 / 修改，改了哪些字段一眼看到，不是整份文档对拷。
- 状态流转：草稿 → 待审核 → 已发布，对应贵司的流程治理制度。

---

## 7. 收尾（30 秒）

> "总结一下：贵司给我一份 Excel 或 Visio，我给回一套**可管理的流程资产 + 可执行的 BPMN + 可发布的 SOP 文档**。
> 再往后，我们可以把整套能力封装成你们自己的工具，让业务部门自己维护。"

---

## 能力清单（对照选型维度）

| 维度 | 本平台能力 | 演示位置 |
| --- | --- | --- |
| 资产建模 | Excel/Visio/Word/PDF/自然语言 → 统一流程资产模型 | 顶部导入 |
| 流程架构 | 资产空间 → L1~Ln 架构树 → 流程清单 | 左侧面板 |
| 多视图 | 泳道图 / BPMN 2.0 / 价值链图 / 资产表格 | 中间视图切换 |
| 标准与集成 | 导出标准 BPMN 2.0 XML；反向解析外部 .bpmn | BPMN 视图导出 |
| 文档生成 | 一键生成 SOP 文档，HTML 预览 / .docx / 打印 PDF | 一键成文 |
| 版本治理 | JSON 快照、节点级 diff、草稿/待审核/已发布 | 版本对比 |
| 智能化 | NL 建模、文档解析、合规诊断；**默认离线、可内网部署** | 右侧诊断面板 |
| 安全合规 | 数据脱敏开关、全离线、无外网依赖 | 设置 |

---

## 常见问题应答

**Q：这些图和 Camunda/Flowable 能接吗？**
能。导出的是标准 BPMN 2.0 XML（含 DI 图形信息），第三方引擎可直接导入；外部 .bpmn 也能反向读进来。

**Q：我们内部系统要集成吗？要开发多久？**
本 Demo 是纯前端原型，不碰客户数据、不接内网系统。生产版为 Java / 信创技术栈 + 后端服务 + 流程引擎。

**Q：AI 诊断是不是大模型？**
默认不是，是规则引擎，可离线在内网跑。接了大模型才会叠加 AI 建议，界面上会明确标注当前模式——我们不把规则输出说成 AI。

**Q：能处理多少流程？**
单文件解析是毫秒级（237 形状 / 102 连线 < 1 秒）。批量处理能力会以 skill / 服务端任务的形式提供。

**Q：数据会不会泄露？**
样例全部脱敏（客户A / 系统A-B-C / 船公司A），脱敏开关在设置里，公网演示默认开启。

**Q：能发给我自己打开看吗？**
可以。`dist/SOP流程资产平台.html` 是自包含单文件，微信/邮件直接发，双击就开，断网也能用，不需要装任何东西。

---

## 边界说明（务必讲，避免后续争议）

- 本 Demo 是**售前原型**，用于验证"存量 SOP → 结构化资产 → 图 → 引擎流程 → 文档"链路的可行性。
- 生产版本为 **Java / 信创技术栈 + 后端服务 + 流程引擎**，包含权限、多租户、审计、与贵司系统的集成，不在本原型范围内。
- BPMN XML 的语义完整性已按 BPMN 2.0 规范生成；若贵司引擎有特殊扩展属性要求，可在导出环节按引擎定制。
